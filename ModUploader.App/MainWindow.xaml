<Window x:Class="ModUploader.MainWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:local="using:ModUploader" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:res="using:ModUploader.Resources"
        mc:Ignorable="d"
        Title="Mod Uploader">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="BackButton"
                    Click="BackButton_Click" 
                    Style="{StaticResource TitleBarBackButtonStyle}" IsEnabled="{x:Bind MainFrame.CanGoBack, Mode=OneWay}"
                    Visibility="Collapsed" 
                    ToolTipService.ToolTip="{x:Bind res:Resources.Back}"/>

            <Grid  x:Name="AppTitleBar" Grid.Column="1">
                <Image Source="ms-appx:///Assets/icon_256x256.png" HorizontalAlignment="Left" Width="16" Height="16" Margin="8,0,0,0"/>
                <TextBlock Text="Mod Uploader" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" Margin="28,0,0,0"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">

            <Frame x:Name="MainFrame" Navigated="MainFrame_Navigated"/>

            <TextBlock x:Name="WatermarkText"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Bottom"
                   Margin="10"
                   Foreground="Gray"
                   FontSize="12"
                   Opacity="0.5"
                   IsHitTestVisible="False"/>
        </Grid>
    </Grid>
</Window>