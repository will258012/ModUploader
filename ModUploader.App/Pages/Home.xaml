<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ModUploader.Pages.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModUploader.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:res="using:ModUploader.Resources"
    mc:Ignorable="d">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20" MinWidth="500" MaxWidth="1000">
            <TextBlock x:Name="TextBlock" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
            <Button x:Name="BtnNew" HorizontalAlignment="Stretch" Height="60" Click="BtnNew_Click">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <SymbolIcon Symbol="Add"/>
                    <TextBlock Text="{x:Bind res:Resources.Home_Create, Mode=OneWay}"/>
                </StackPanel>
            </Button>
            <Button x:Name="BtnUpdate" HorizontalAlignment="Stretch" Height="60" Click="BtnUpdate_Click">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <SymbolIcon Symbol="Edit"/>
                    <TextBlock Text="{x:Bind res:Resources.Home_Update, Mode=OneWay}"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
