<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ModUploader.Pages.Splash"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModUploader.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:res="using:ModUploader.Resources"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="30">
            <Image MaxHeight="128" MaxWidth="128" Source="/Assets/icon_256x256.png" Stretch="Uniform"/>
            <ProgressRing x:Name="ProgressRing" IsActive="True" />
            <TextBlock x:Name="ErrorTextBlock" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" Foreground="Red" Visibility="Collapsed"/>
            <Button x:Name="BtnRetry" Click="BtnRetry_Click" HorizontalAlignment="Center" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <SymbolIcon Symbol="Refresh"/>
                    <TextBlock Text="{x:Bind res:Resources.Retry, Mode=OneWay}"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
